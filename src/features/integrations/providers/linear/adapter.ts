import { linearMockItems } from "@/features/integrations/mock/linear.mock";

export async function getLinearItems() {
  // TODO: Replace mock data with Linear GraphQL reads for assigned/high-priority issues.
  return linearMockItems;
}
